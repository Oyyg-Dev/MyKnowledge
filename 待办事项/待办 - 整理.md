

1. 简化后的内容如下：
+ **数据洞察**：
    - 每个事业部的提交人员数量、项目和统计数据。
    - 按事业部、月份及提案类型进行维度分析。
+ **创新平台对接**：
    - 确认立项时能否接收数据。
    - 使用`<include refid="selectInpProposalVo"/>`查询，并通过`LEFT JOIN`连接`sys_dept`与`sys_user`表以获取部门信息和创建者信息。

```plain
<include refid="selectInpProposalVo"/>
LEFT JOIN sys_dept d ON p.dept_no = d.dept_id
LEFT JOIN sys_user u ON p.create_by = u.user_name
```

+ **创新平台优化**：
    - 清理过期数据。
    - 在OA系统中，立项和提案记录需保存`fdid`字段。
    - OA立项过程中，增加存储审核人信息的功能。
    - 调整PC端管理界面。
2. 调整，pc端的管理  
  
  
  
2026/04/10  
  
立项，最后状态未更新为已完成 ，导致任务按照未显示  
创新组长 和秘书处审批 人未同步 显示  
数据分析 ：增加角色事业部区分，权限筛选  
公告区：增加三个模块类型，动态增加类型，筛选  
创新之星的计分逻辑，整理出来

使用文档(小程序和OA),流程文档  
创新之星的计分逻辑，整理出来

使用文档(小程序和OA),流程文档

测试数据删掉

小程序立项，单点登录OA立项页面去提交

增加角色事业部区分， 筛选整个创新平台数据权限

提案库：增加审批人可以查看审批的流程数据 

